# Changelog

## [2026-08-28]
### Fixed
- Исправлена работа Яндекс Карт и отображение меток дилерских центров на главной странице (`main.dealerships`):
  - Добавлена безопасная инициализация `window.YAPP` перед передачей серверных данных `YAPP.DEALERSHIPS` в `template.php`.
  - Реализован механизм ожидания загрузки API Яндекс Карт (`waitYmapsReady`) с подпиской на `ymaps.ready()`, исключающий гонку скриптов при первой загрузке страницы.
  - Карта инициализируется единожды, а при переключении табов («Все», «Новые», «С пробегом», «Сервис», «Центры выкупа», «Выездной выкуп»), выборе города в шапке (`city:changed`) или фильтрации в дропдаунах коллекция меток обновляется нативно (`removeAll()` / `add()`) с центрированием `setBounds()`.
  - Устранена потеря исходного массива `ITEMS` при повторной фильтрации в `buildVew()`.
  - Устранена ошибка PHP 8.x `TypeError: Cannot access offset of type string on string` при клике на метку ДЦ: в `script.js` отправка данных карточки переведена на `application/json`, а в методе `YAApi::apiRenderMainDealershipView` добавлены строгие проверки типов массивов `SITE` и `BUTTONS`.

## [2026-08-21]
### Added
- Добавлен эффект плавного увеличения картинки (`transform: scale(1.08)` с `transition: transform var(--transition)`) при наведении курсора на карточки в списках блога (`news/blog`), новостей (`news/news`), спецпредложений (`news/offers`) и слайдера новостей на главной (`news.list/main.news`) по аналогии с карточками автомобилей.
- В класс ядра `YApp` добавлена константа инфоблока блога `const IBLOCK_BLOG = 26;`.

### Changed
- Проведен комплексный рефакторинг раздела `/blog/` и шаблона `bitrix:news` (`blog`):
  - В блоке фильтра блога реализован вывод исключительно тех тегов, к которым привязана хотя бы одна активная статья (исключены пустые теги и переходы на 404/пустые страницы).
  - Реализовано точное сопоставление символьных кодов `XML_ID` тегов с `ID` значений списка в фильтре `$GLOBALS['arFilterBlog']` в `news.php`.
  - Включено кэширование блока «Читайте также» на детальной странице статьи (`CACHE_TYPE => A`).
  - Включены канонические URL (`DETAIL_SET_CANONICAL_URL => Y`) и обработка 404 ошибок на уровне компонента.
  - Удалены инлайн-стили `<style>` из `blog/index.php`, класс фона страницы задается через `$APPLICATION->SetPageProperty()`.
  - Добавлена строгая проверка типов для PHP 8.1+ (`str_replace`, `explode`, `is_array`).

### Fixed
- Исправлена фатальная ошибка PHP 8.x `TypeError: array_chunk(): Argument #1 ($array) must be of type array, null given` в общем шаблоне карточки автомобиля `local/templates/yugavto.theme.2025/include/item_vehicle.php`: добавлена строгая проверка и инициализация массивов `$item['_general']`, `$item['_tags']`, `$data['FAVORITES']`, `$data['COMPARE']`.

## [2026-08-19]
### Fixed
- Исправлена работа выпадающих списков фильтра витрины (`cars/views/filter/select.php`):
  - Добавлен атрибут `data-link="true"` на контейнеры `.form-droplist` в шаблоне витрины.
  - Скорректирован глобальный обработчик кликов по элементам выпадающих списков в `forms.js` и `bundle.app.min.js`, исключающий блокировку нативных ссылок перехода на витрине и в связанных фильтрах.
- Исправлена работа фильтра на главной странице (`main.filter`):
  - Реализовано автозакрытие выпадающего списка марки/модели при выборе пункта (`d-none`, снятие `form-dropdown-opened`).
  - Исправлен сброс заголовков в селектах: при 1 марке отображается название бренда, при 2+ — «Марка: N выбрано».
  - Исправлена подгрузка и рендеринг списка моделей в селекте при выборе одной или нескольких марок.
  - Настроена актуализация мультирейндж слайдера цен (min/max границы, смещение ползунков, значения «от» и «до») и ссылки кнопки с количеством авто.
  - Добавлена корректная обработка сброса крестиком (`.before`) в шапке селектов марки и модели.

## [2026-08-18]
### Changed
- Переведена структура `sitemap.xml` на плоский одноуровневый индекс: файлы `sitemap-brands-*.xml` и `sitemap-vehicles-*.xml` внедряются напрямую в `sitemap.xml` без промежуточных индексных файлов `sitemap-cis-*.xml`.
- Актуализированы директивы `Allow:` в `robots.txt` под плоскую структуру карт сайта.

### Fixed
- Исправлена ошибка формирования URL в `cron/makeSitemap.php` при отправке запросов в Google Indexing API (удален ошибочный закрывающий тег `</url>`).
- Добавлена директория `/cron/IndexingLog/` в `.gitignore`.

### Removed
- Удалены устаревшие неактуальные sitemap-файлы инфоблоков (`sitemap-iblock-1,2,3,5,6,7,8,9,10,17.xml`) и бэкап `~1sitemap-files.xml`.
- Удалены устаревшие неиспользуемые XML/YML выгрузки авто (`vehicles-new-02.xml`, `vehicles-new-krd.xml`, `vehicles-new-nvr.xml`, `vehicles-new.xml`, `vehicles-new.yml`, `vehicles-used-krd.xml`, `vehicles-used-nvr.xml`).
- Удалены устаревшие тестовые разделы `dealerships/test/`, `offers/test/`, `about/history/test/`.
- Удалена устаревшая директория `/about/log_request/` с логами запросов.

## [2026-08-17]
### Fixed
- Перенесена валидация ответа Go API, условий 404 и редиректов 301 до вызова `prolog_after.php` в `/cars/new/index.php` и `/cars/used/index.php`, устранив фатальные ошибки `headers already sent` и отдачу HTTP 500 под PHP 8.4.
- Защищен цикл `foreach` в `/cars/views/vehicles.php` проверкой `is_array()` от падения при пустом результате каталога.
- Файл `robots.txt` исключен из отслеживания Git и добавлен в `.gitignore` для независимого управления на разных окружениях (DEV/PROD).
- Устранены предупреждения `PHP Warning: Undefined array key "mode"` и неинициализированный массив `$G` при редиректах устаревших параметров в `/cars/new/index.php` и `/cars/used/index.php` под PHP 8.4.

## [2026-08-17]
### Added
- Создан и дополнен интерактивный HTML-отчет `complectation_analysis.html` в корне DEV-сервера холдинга с анализом внедрения уровня «Комплектация» в витрину (архитектура Bitrix + Go API, детальное сравнение ЧПУ vs GET-параметров конкурентов Рольф/Авторай, гибридная модель маршрутизации и технический план работ).

## [2026-08-07]
### Added
- Внедрена дефолтная автоматическая генерация канонического тега `<link rel="canonical" href="...">` на самого себя (self-canonical) в `header.php` для всех статических страниц сайта (`/about/`, `/about/career/`, `/corporate/`, `/services/` и т.д.) с отсечением служебных GET-параметров (UTM-меток, gclid и др.).
- Настроено строгое разделение каталога `llms.txt`: в блок «Новые авто у официального дилера» включены строго 29 официальных брендов дилера новых авто и все их модели; в блок «Автомобили с пробегом» включены б/у бренды (BMW, Audi, Mercedes и др.) и их модели.
- Переведена генерация уникального мета-тега `title` при пагинации на сторону Go API; клиенты витрин (`/cars/new/` и `/cars/used/`) получают единый `meta.title` из Go API с суффиксом ` — Страница #N`.
- Внедрена микроразметка Schema.org (`Blog`, `BlogPosting`, `NewsArticle`/`Article`, `Organization`, `WebPage`, `PostalAddress`) на странице списка новостей (`/news/`), на детальных страницах новостей, на детальных страницах автосалонов и на главной странице (`main.news`).
  - Добавлены рекомендуемые свойства `NewsArticle`: `headline`, `description`, `image`, `author`, `publisher`, `datePublished`, `dateModified`, `mainEntityOfPage`, `articleSection`, `keywords`, `inLanguage`.
- Реализован рефакторинг сайта холдинга на Bitrix D7 (согласно `refactoring_plan_d7.md`):
  - Создан архитектурный базис `Local\Project` (`Handlers\SEO`, `Services\LastModified`, `Services\PhoneService`, `Services\FilterService`).
  - `init.php`: 301-редиректы переведены на D7-событие `OnBeforeProlog`, вычисление `Last-Modified` переведено со старого SQL `$DB->Query` на D7 ORM `\Bitrix\Iblock\ElementTable::getList()` с кэшированием.
  - `header.php`: устаревшие вызовы `CModule::IncludeModule` заменены на `\Bitrix\Main\Loader::includeModule()`, выгрузка контактов из Highloadblock вынесена в `PhoneService::getContacts()` с кэшем.
  - `footer.php`: для подвала включено управляемое кэширование компонентов (`CACHE_TYPE => A`).
  - Компоненты `main.filter` и `footer`: выборка историй (stories) и SEO-текстов переведена с `CIBlockElement::GetList` на D7 ORM c кэшированием `\Bitrix\Main\Data\Cache`.
  - Страницы `/dealerships/` и `/news/`: убраны прямые вызовы старого ядра, выгрузка фильтров вынесена в `FilterService`, включено кэширование компонентов.

- Реализован рефакторинг клиентского JS-стека сайта холдинга (согласно `refactoring_plan_js.md`):
  - **`YAppStore`**: Создан событиями управляемый реактивный стор (`store.js`) на базе нативного `EventTarget` с Pub/Sub событиями `city:changed`, `entity:changed`, `favorites:updated`, `compare:updated`.
  - **Ликвидация 8 вечных таймеров `setInterval(100)`**: Полностью вырезаны все фоновые опросники в компонентах `menu/top`, `main.compilations`, `main.dealerships` и `main.filter`.
  - **Отмена сетевых запросов**: Внедрен нативный `fetch()` с `AbortController` для мгновенной отмены зависших AJAX-запросов подборок авто.
  - **Изолированный менеджер форм `FormHandler`**: Модульный класс `form-handler.js` заменил устаревший `forms.js`, устранены все глобальные утечки переменных (`let form, sendData = {}, flag = true`).

- Составлен и сохранен детальный план архитектурного PHP-рефакторинга витрины `/cars/` (`refactoring_plan_cars_php.md` в корне сайта) в автономное независимое приложение `YugAvto\Showroom` (PHP 8.2+, PSR-4/PSR-7/PSR-18, DTO, Standalone Router) с поддержкой легковесных PHP-виджетов для лендингов (`ShowroomWidget`), кастомной верстки/стилей, REST API фильтрации по маркам/моделям/дате (`WidgetApiAdapter`), службами Избранного и Сравнения. Файл добавлен в `.gitignore`.






### Fixed
- Исправлена ошибка валидации микроразметки `itemprop="email"` для Schema.org: атрибут перенесен с тега ссылки `<a>` с префиксом `mailto:` на внутренний тег `<span>` с текстом адреса электронной почты.
- Устранена ошибка HTTP 500 в скрипте `cron/makeSitemap.php` (подключен класс `YApp` и добавлено безопасное отключение генерации системных ошибок с поддержкой `?debug=1`).
- Исправлена микроразметка `ListItem` в компоненте `bitrix:breadcrumb`:
  - Вложенность `ListItem` приведена в строгое соответствие со стандартом `BreadcrumbList`.
  - Для всех элементов гарантирована абсолютная ссылка `itemprop="item"` с понятными свойствами `name` и `position`.

## [2026-08-04]
### Added
- Реализована генерация мобильного диплинка (`max://share?text=...`) для расшаривания карточки сотрудника в мессенджере МАКС (`Employees.php`, `/employees/index.php`).

### Fixed
- Исправлена генерация URL в методе `makeFilterUrl` (`YApp.Showroom.class.php`):
  - При выборе одной модели формируется ЧПУ вида `/cars/new/avatr-11/` (или `/cars/new/avatr/avatr-11/`) без передачи параметров через GET.
  - Сохранён перенос модели в GET-параметр (`?model=avatr-11,avatr-12`) только при мультивыборе (2 и более моделей).

### Changed
- Внедрена нормализация и суммирование скидок на детальной странице авто (`cars/views/vehicle/vehicle.php`):
  - Приведение названий скидок к читаемым категориям («Трейд-ин», «Кредит», «Семейный автомобиль», «КАСКО в подарок», «Прямая скидка» и др.).
  - Автоматическое объединение скидок одного типа и суммирование их значений.

## [2026-08-03]
### Fixed
- Исправлена пагинация витрин: исключен суффикс page-1/ при генерации ссылок на 1-ю страницу в makeFilterUrl.
- Реализован 301 редирект с адресов c page-1/ на соответствующий корень витрины.

## [2026-08-03]
### Changed
- Модернизирована микроразметка Schema.org детальной страницы авто под стандарт JSON-LD (по образцу Авто.ру):
  - Переведено на единую сущность Product в формате JSON-LD с указанием NewCondition, спецификациями комплектации, фото и продавцом AutoDealer (Официальный дилер Юг-Авто).
  - Из верстки карточки товара (vehicle.php) удалены устаревшие атрибуты Microdata (itemscope, itemprop, itemtype).

 - yug-avto.ru (Холдинг)

## [2026-07-31]
### Added
- Настроен 301-редирект для index.php, index.html и index.htm на корень / на уровне PHP в `local/php_interface/init.php` (для предотвращения конфликтов/перезаписи при слиянии .htaccess).

### Changed
- Прописаны корректные атрибуты `alt` и `title` для изображений акций/историй (Stories) в компоненте `news.list/main.filter`, а также для логотипов брендов и иконок в компоненте вывода истории холдинга `news.list/history`.
- Удалена дублирующая микроразметка Schema.org (Product, name, description) из карточек других автомобилей `others_vehicle.php` и рекомендованных автомобилей `recomended_vehicle.php`, так как они выводятся внутри детальной страницы автомобиля и ломают семантику главного Product-объекта.
- Обновлено формирование атрибутов `alt` и `title` для изображений автомобилей (аналогично Эксперту):
  - В списочных карточках авто (каталог, избранное, сравнение, рекомендации, другие авто) они теперь генерируются строго из наименования (Бренд + Модель) с добавлением индекса ракурса.
  - На детальной странице автомобиля они формируются на основе H1-заголовка, включающего бренд, модель, год выпуска и пробег.

## [2026-07-29]
### Added
- Добавлен нетрекаемый конфигурационный файл `/local/php_interface/yapps_config.php` для локальной настройки доменов Go API на DEV и PROD серверах.
- В `.gitignore` добавлены правила исключения временных файлов настроек домена, логов и файлов ключей `.json`.

### Changed
- Класс `YApp` переведен на наследование от `YAppConfig` для автоматической подгрузки доменов без перезаписи при слиянии Git-веток.
- Настроен беспарольный пуш и деплой на GitHub по SSH напрямую с серверов разработки (`serverDEV`) и прод-сервера (`Holding`).
